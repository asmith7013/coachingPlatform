name: Scrape Roadmaps Daily

on:
  # Runs daily at 2 AM UTC (9 PM EST / 6 PM PST)
  schedule:
    - cron: '0 2 * * *'

  # Allows manual trigger from GitHub Actions tab
  workflow_dispatch:

jobs:
  scrape-roadmaps:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium

      - name: Run Scraper
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          ROADMAPS_EMAIL: ${{ secrets.ROADMAPS_EMAIL }}
          ROADMAPS_PASSWORD: ${{ secrets.ROADMAPS_PASSWORD }}
        run: |
          echo "🚀 Starting roadmaps scraper..."
          npx tsx scripts/run-scraper.ts

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Scraping failed. Check the logs above for details."
          echo "💡 You can re-run this workflow manually from the Actions tab."
