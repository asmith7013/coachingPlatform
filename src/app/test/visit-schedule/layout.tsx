import React from 'react';

export default function VisitScheduleTestLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="visit-schedule-test-layout">
      {children}
    </div>
  );
} 